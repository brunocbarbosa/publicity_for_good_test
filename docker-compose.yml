version: '3'

services:
  bet-api-pg:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=product
      - POSTGRESQL_PASSWORD=product
      - POSTGRESQL_DATABASE=productapi